# ============================================
# INSTRUÇÕES DE USO
# ============================================
# 1. Copie este arquivo para .env
# 2. Preencha todas as variáveis abaixo
# 3. NUNCA faça commit do arquivo .env
# ============================================

# ============================================
# 🔐 CONFIGURAÇÕES DE SEGURANÇA (OBRIGATÓRIO)
# ============================================

# Chave de criptografia - MÍNIMO 32 caracteres
# Para gerar: openssl rand -hex 32
N8N_ENCRYPTION_KEY=COLE-AQUI-SUA-CHAVE-DE-32-CARACTERES

# JWT Secret para autenticação
# Para gerar: openssl rand -hex 32
JWT_SECRET=COLE-AQUI-SEU-JWT-SECRET

# Senha do banco de dados PostgreSQL
# Use uma senha forte!
POSTGRES_PASSWORD=senha-forte-postgres-2025

# ============================================
# 🌐 CONFIGURAÇÕES DE ACESSO (OBRIGATÓRIO)
# ============================================

# Seu domínio ou IP público
# Exemplos: 
# - n8n.meusite.com.br
# - 192.168.1.100
# - localhost (apenas para teste local)
N8N_HOST=localhost

# Protocolo de acesso
# Use 'https' se tiver SSL configurado
# Use 'http' para teste local
N8N_PROTOCOL=http

# URL completa para webhooks
# Deve corresponder ao HOST e PROTOCOL acima
WEBHOOK_URL=http://localhost:5678/

# ============================================
# 📧 CONFIGURAÇÕES DE EMAIL (OPCIONAL)
# ============================================

# Modo de email (smtp ou deixe vazio para desabilitar)
EMAIL_MODE=

# Configurações SMTP (exemplo com Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=seu-email@gmail.com
# Para Gmail: use uma "Senha de App", não sua senha normal
# Veja: https://support.google.com/accounts/answer/185833
SMTP_PASS=sua-senha-de-app
SMTP_SENDER=seu-email@gmail.com

# ============================================
# ⚙️ CONFIGURAÇÕES AVANÇADAS (OPCIONAL)
# ============================================

# Timezone (fuso horário)
TIMEZONE=America/Sao_Paulo

# Modo de execução
# - regular: padrão, executa tudo em um container
# - queue: avançado, usa Redis para fila de execução
EXECUTIONS_MODE=regular

# Tempo de retenção de execuções em horas
# 168 = 7 dias, 720 = 30 dias
EXECUTIONS_DATA_MAX_AGE=168

# Nível de log (error, warn, info, verbose, debug)
LOG_LEVEL=info
